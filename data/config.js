const wsqls = {
    user : 'luis',
    password :'qwerty',
    server: '10.0.0.10',
    database: 'api',
    options:{
        trustserverCertificate: true,
    },
};
/*const mysql = require('mysql');

const config = {
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'api',
};

const pool = mysql.createPool(config);

module.exports = pool;*/
